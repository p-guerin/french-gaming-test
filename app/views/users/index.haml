%h1 Membres de la team

-@users.each do |u|
  %ul
    %a{:href => "users/#{u.id}"}
      %li= u.username
    %li= u.email
    %li= u.password_digest
    %li= u.age
    %li= u.city
    %li= u.country
    %li= u.image
    %li= u.steam
    %li= u.facebook
    %li= u.twitter
    %li= u.youtube
    %li= u.created_at
    %li= u.updated_at
  - if current_user.admin? && !current_user?(u)
    = link_to "delete user", u, method: :delete, data: { confirm: "You sure?" }